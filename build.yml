build:
  version: 2016-03-14
  steps:
    base:
      name: base
      dockerfile: Dockerfile.base
    deployment:
        name: deployer
        dockerfile: Dockerfile.deployment
        depends_on:
          - base
